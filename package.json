{
  "name": "cross-language-tests",
  "version": "1.0.0",
  "description": "Cross-language test suite for BRC-104 signature compatibility",
  "main": "test_brc104_signatures.ts",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": ["**/test_*.ts"],
    "collectCoverageFrom": ["**/*.ts", "!**/node_modules/**"]
  }
}

